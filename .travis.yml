language: node_js
node_js: node
cache: npm
sudo: required
services:
  - docker

before_install:
  - docker build -t frontend -f Dockerfile.dev . 

script:
  - docker run -e CI=true frontend npm run test
    